<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Chef Routes
|--------------------------------------------------------------------------
| These routes are loaded by the RouteServiceProvider within the api group.
| Prefix: /api
| Middleware: api
|
| Add chef-specific routes here (typically protected by auth:api + role:chef).
| Example:
|   Route::middleware(['auth:api', 'role:chef'])->group(function () {
|       Route::get('/dashboard', ...);
|   });
*/
